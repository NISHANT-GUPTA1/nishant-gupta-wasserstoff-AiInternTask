from app import app  # Replace 'app' with the filename of your Flask app module without the .py extension

if __name__ == "__main__":
    app.run()  # Only needed if running directly, optional for deployments